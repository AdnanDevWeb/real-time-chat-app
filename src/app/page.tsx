import React from 'react'
import { db } from '@/lib/db'
import Button from '@/components/ui/Button'
const page = async () => {
  return (
    <Button variant={'ghost'} size={'default'}>Suu</Button>
  )
}

export default page